# telegram-777-monitor

Cleaned, comment-free version of the interactive monitor script with dynamic buttons.
Place your credentials in `.env` and run:

```bash
pip install -r requirements.txt
python interactive_monitor_777_with_buttons.py
```

Edit `settings.json` to change templates, target results account, or hit counts.
